 History
 :Access Public shared
⍝ * 3.1.0
⍝   * Icon improved.
⍝ * 3.0.5
⍝   * Bug fixes:
⍝     * Entering a, say, special character like "&" into "Search for" led to an error message even when "Search as entered" was ticked.
⍝     * Searching for `<`, `>` and `&` did not work correctly because they were &gt; etc. internally the moment the search was performed.
⍝\\
⍝ For information regarding older versions see <https://github.com/aplteam/Laguntza/releases>
