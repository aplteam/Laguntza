 R←Test_020(stopFlag batchFlag);⎕TRAP;ref;ps;css
⍝ Test with an empty namespace: cannot work of course, so we expect an error.
⍝ R gets one of: 0=Okay, 1=test case failed, ¯1=test case was not executed due to the "batchFlag"
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 #.⎕SHADOW'TestHelp'
 'TestHelp'#.⎕NS''

 ps←##.Laguntza.CreateParms ⍬
 ps.helpCaption←'TestCases'
 ps.folderName←GetPathForTempHelpProject↑¯1↑'.'#.APLTreeUtils.Split 0⊃⎕XSI
 ps.source←#.TestHelp
 ps.notifyAcre←0
 ps.regPath←0

 :Trap 11
     ref←1 ##.Laguntza.New ps
     →GoToTidyUp 1
 :Else
     →GoToTidyUp'The source namespace is empty!'≢↑⎕DM
 :EndTrap

 R←∆OK

∆TidyUp:
 ##.Laguntza.Reset
 ⎕EX'#.TestHelp'
