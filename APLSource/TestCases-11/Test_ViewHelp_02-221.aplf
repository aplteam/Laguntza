 R←Test_ViewHelp_02(stopFlag batchFlag);⎕TRAP;ref;report;path;tempPath;rc;processInfo;result;more;parms;pid;flag;myZipper;success
⍝ Test relative (internal) paths with `ViewHelp.exe`. Was a bug introduced with version 2,0.0
⍝ R gets one of: 0=Okay, 1=test case failed, ¯1=test case was not executed due to the "batchFlag"
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 :If batchFlag
     R←∆NoBatchTest
     :Return
 :EndIf

 R←∆Failed

 path←#.FilesAndDirs.NormalizePath #.FilesAndDirs.PWD,'\Dist\'
 tempPath←#.FilesAndDirs.GetTempFilename''
 #.FilesAndDirs.DeleteFile tempPath
 tempPath←¯4↓tempPath
 success←'Create!'#.FilesAndDirs.CheckPath tempPath
 'Something went terribly wrong'Assert success

 myZipper←⎕NEW #.SevenZip(,⊂path,'Laguntza-',({⍵/⍨2≥+\⍵='.'}1⊃##.Laguntza.Version),'.zip')
 rc←myZipper.Unzip tempPath
 'Something went terribly wrong'Assert 0=↑rc

 parms←#.Execute.DefaultParms
 parms.wait←0
 (rc processInfo result more)←parms #.Execute.Application tempPath,'/ViewHelp.exe'
 'Something went terribly wrong'Assert 0=rc
 pid←2⊃processInfo

 R←~∆YesOrNo'Now click on the "Names and conventions" link - does it jump to the right page?'
 {}#.OS.KillPID pid
 →GoToTidyUp R

∆TidyUp:
 #.FilesAndDirs.RmDir tempPath
