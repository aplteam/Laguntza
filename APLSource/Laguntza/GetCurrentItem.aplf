 GetCurrentItem←{
     n←⍵
     {⍵<≢n.tree.SelItems:⍵ ⋄ 1}⍬⍴n.tree.SelItems⍳1
⍝ Returns the number of the current item as a scalar
⍝ or 1 if there is no item selected currently.
⍝ Yes, that DID happen in the past.
 }
